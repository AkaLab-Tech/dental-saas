{
  "_comment": "Per-project atelier configuration. Created by /atelier:setup-project. The operator may edit this file; atelier never overwrites it once it exists. To reset to defaults, delete the file and re-run /atelier:setup-project.",
  "prSize": {
    "_comment": "Per-PR size budget enforced by atelier-pr-size-check (M7.1.F27). A PR is OVERSIZE when BOTH limits are exceeded after exempting auto-generated / test / migration files. Tune these as your project's reviewability tolerance evolves; very small repos may want stricter values, monorepos may want larger ones.",
    "maxLines": 200,
    "maxFiles": 10,
    "exempt": [
      "pnpm-lock.yaml",
      "package-lock.json",
      "yarn.lock",
      "**/*.snap",
      "**/*.generated.*",
      "**/*.test.*",
      "**/*.spec.*",
      "**/__tests__/**",
      "**/tests/**",
      "**/e2e/**",
      "**/playwright/**",
      "**/cypress/**",
      "**/migrations/**",
      "**/*.sql"
    ]
  },
  "taskDecomposer": {
    "_comment": "Autonomous task decomposition (M4.24.a defines the Epic / sub-task format; M4.24.b adds the agent that auto-rewrites large tasks into epics before implementer runs). Set `enabled: false` to turn off the automatic pass — the operator can still invoke /atelier:slice-task <id> manually. The orchestrator's heuristic for `oversize-likely` only fires when this flag is true.",
    "enabled": true
  },
  "decisionPolicy": {
    "_comment": "Per-project policy for the decision broker (M4.26). Each catalog category maps to one of three values: \"auto\" (an evaluator agent decides per-case — Haiku/Sonnet/Opus depending on the category's riskLevel), \"ask\" (atelier asks the operator like it does today), or a fixed option id (e.g. \"fix-first\" — always pick that option without thinking). `default` covers categories not listed individually. Generated by `/atelier:setup-project` interactive prompts (run `/atelier:set-policy` to revise without re-running setup). The full catalog of categories lives at $CLAUDE_PLUGIN_ROOT/agents/decision-broker/catalog.json — operators do NOT edit the catalog. Fresh-install default is `ask` for everything; M4.26.a base ships dormant and integration wires through this block when M4.26.c lands.",
    "default": "ask",
    "byCategory": {}
  }
}
